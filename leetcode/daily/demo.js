/**
 * @author chenlingyu
 */
const util=require("../utils")

const ps=[]

const ex = () => {
    const must = [];
    return {
        lc: (args) => {
            let answer = args
            return answer
        }
    }
}

util.run(ps,ex)